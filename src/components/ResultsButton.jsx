import { useState } from 'react';
const ResultsButton = props => {
	const { props1, setProps } = useState({ props });
	const HandleClick = props1 => {};
	localStorage.setItem('testname', props.organization);
	return <></>;
};
export default ResultsButton;
